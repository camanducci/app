import { Injectable } from '@angular/core';

@Injectable()
export class TransactionProvider {

  constructor() {
  //  if( this.auth.currentUser() !== null ) 
//      this.PATH = this.PATH+"/"+this.auth.currentUser().uid;
  }

  getAll() {

  }

  save(object: any) {
    
   
  }
}